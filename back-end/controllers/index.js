const user = require('./user')
const board = require('./bord')
const card = require('./card')
const list = require('./list')
const auth = require('./auth')

module.exports = {
  user,
  board,
  card,
  list,
  auth,
}